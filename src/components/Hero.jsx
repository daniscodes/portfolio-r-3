import { motion } from 'framer-motion';
import { styles } from '../styles';
import { computerCanvas } from './canvas';

const Hero = () => {
  return (
    <section cl>

    </section>
  )
}

export default Hero